name: ui_tool
description: With UI Tool you can easily make your app responsive for different screen sizes.
version: 1.0.0
homepage: https://github.com/kazcorei/ui_tool

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

repository: https://github.com/kazcorei/ui_tool

flutter:
