name: ui_tool
description: With UI Tool you can easily make your app responsive for different screen sizes.
version: 0.1.1
homepage: https://github.com/kasun-u/ui_tool

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

repository: https://github.com/kasun-u/ui_tool

flutter:
